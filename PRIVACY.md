# 隐私权政策（Privacy Policy）

生效日期：2025-12-30

本隐私权政策适用于浏览器扩展程序「GSC 16个月备份（非官方）/ Search Console 16‑Month Backup (Unofficial)」（下称“本扩展”）。本扩展用于在用户授权后，从 Google Search Console（GSC）以只读方式读取数据，并将数据归档/导出到用户本地，以便保留超过约 16 个月的历史记录。

## 1. 我们收集/处理哪些数据

本扩展仅在实现其单一用途（本地归档 GSC 数据）所必需的范围内处理数据：

### 1.1 身份验证信息（OAuth）
- 本扩展通过 Chrome 的 `chrome.identity` 发起 Google OAuth 授权并获取访问令牌（Access Token），用于调用 Google Search Console API。
- 访问令牌由浏览器授权流程提供与管理；本扩展不要求用户输入密码，也不以明文形式收集或存储用户密码。

### 1.2 网站内容 / Search Console 数据
在用户授权并选择站点（Property）与时间范围后，本扩展可能会读取并在本地保存以下类型的 GSC 数据（取决于用户在界面中选择的维度与导出/归档选项）：
- 站点 Property（站点标识/URL）
- 指标：点击（clicks）、展现（impressions）、点击率（ctr）、平均排名（position）
- 维度（可选）：按月汇总（totals）、查询词（query）、页面（page / URL）、设备（device）、国家/地区（country）、搜索结果展现类型（searchAppearance）、日期（date）

## 2. 数据如何使用
- 仅用于：向 Google 官方 API 请求并展示/归档/导出用户选择的 GSC 数据。
- 不用于：广告投放、用户画像、跨站跟踪、出售或转让数据、征信或贷款相关用途。

## 3. 数据存储位置与保留期限

### 3.1 本地存储
- 归档数据与扩展配置默认保存在用户浏览器本地的扩展存储中（`chrome.storage.local`）。
- 我们不提供服务器端存储；默认情况下数据不会上传到开发者或任何第三方服务器。

### 3.2 导出到文件（可选）
- 若用户在扩展中启用“导出/下载”相关功能并授予可选下载权限，本扩展会将用户选择的数据导出为文件并保存到用户设备（通常为浏览器下载目录）。

### 3.3 保留期限
- 本地归档数据会保留在用户设备上，直到用户主动删除（例如：清除扩展数据、卸载扩展、或在浏览器设置中清除站点/扩展存储）。

## 4. 数据共享与披露
- 我们不会向第三方出售、出租或传输用户数据。
- 本扩展仅与 Google 官方服务通信以获取 GSC 数据（例如 `searchconsole.googleapis.com`、`www.googleapis.com`）。
- 除法律法规要求外，我们不会披露用户数据。

## 5. 权限说明（与隐私相关）
- `identity`：用于 Google OAuth 登录授权与获取访问令牌。
- `storage`：用于在本地保存归档数据与扩展设置。
- `alarms`：用于用户启用的自动归档定时任务。
- `downloads`（可选）：仅在用户启用“保存为本地文件/自动下载导出”时使用。
- 主机权限（host permissions）：仅用于请求 Google 官方 API 以读取用户授权范围内的 GSC 数据。

## 6. 远程代码
- 本扩展不使用远程代码（不从远程服务器下载并执行脚本/代码）。
- 扩展运行所需的代码与资源随扩展包一并发布。

## 7. 数据安全
我们采取合理措施减少数据泄露风险（例如：数据默认仅在本地保存、网络请求仅指向 Google 官方 API 域名）。但请理解：任何系统都无法保证绝对安全。建议用户妥善管理本机账户与浏览器环境。

## 8. 儿童隐私
本扩展不面向 13 岁以下儿童提供服务，也不会主动收集儿童个人信息。

## 9. 本政策的变更
如本扩展的功能或数据处理方式发生变化，我们将更新本隐私权政策并在发布新版本时一并提供更新内容。

## 10. 联系方式
如对本隐私权政策有任何疑问或请求，请联系：
- 邮箱：stormrobin50@gmail.com

